<view class="container">
    <view class="title">单位转换器</view>
    <picker mode="selector" range="{{units}}" bindchange="onUnitChange">
        <view class="picker">
            当前转换类型: <span class="selected-unit">{{selectedUnit}}</span>
        </view>
    </picker>
    <input type="number" placeholder="输入要转换的数值" bindinput="onInput" class="input-field" />
    <button bindtap="convert" class="convert-button">转换</button>
    <view class="result-display">
        <text>{{result}}</text>
    </view>
    <view class="info-tip">
        支持更多类型转换: 温度(摄氏度与华氏度)、体积(升与加仑)
    </view>
</view>
    